name: Build APK

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4

      - name: Free Disk Space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9' # 降级 Python 到 3.9，兼容性更好

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            git \
            python3-pip \
            python3-setuptools \
            python3-venv \
            libffi-dev \
            libssl-dev \
            autoconf \
            libtool \
            pkg-config \
            zlib1g-dev \
            libncurses5-dev \
            libncursesw5-dev \
            libtinfo5 \
            cmake \
            libffi-dev \
            libssl-dev \
            zip \
            unzip \
            openjdk-17-jdk \
            automake \
            gettext \
            autopoint \
            libsdl2-dev \
            libsdl2-image-dev \
            libsdl2-mixer-dev \
            libsdl2-ttf-dev

      - name: Install Buildozer
        run: |
          pip install --upgrade pip
          # 关键修改：强制安装特定版本的 buildozer 和 cython，并安装 python-for-android
          pip install buildozer==1.5.0 cython==0.29.33 python-for-android==2023.6.0

      - name: Build with Buildozer
        run: |
          # 修复 Cython 问题：手动创建头文件链接
          # 这是一个已知的 hack，用于解决 Pygame 在新环境下找不到 longintrepr.h 的问题
          yes | buildozer android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: package
          path: bin/*.apk
